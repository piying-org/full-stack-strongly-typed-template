import { DefaultViewTypes } from './default-type-config';
import { DefaultViewWrappers } from './default-wrapper-config';

export const DefaultViewConfig = {
  types: DefaultViewTypes,
  wrappers: DefaultViewWrappers,
};
